# WeatherPhone Pro

Android weather app built with Kotlin and Jetpack Compose.

The repository includes a GitHub Actions workflow that builds the debug APK and uploads it as the `app-debug.apk` artifact.

## Build

Open the Actions tab and run `Android Debug Build`, or build locally with:

```bash
gradle assembleDebug
```
